import 'package:flutter/material.dart';

class FovoriteScreen extends StatelessWidget {
  const FovoriteScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'Favorite movies'
        ),
      ),
    );
  }
}
